Tell me which MCP-backed tools are available right now.

Use only already-configured tools. Do not search the MCP catalog, install an MCP server, or change MCP configuration.

Group the available tools by namespace:

- `mcp_netops_rag_*`
- `mcp_network_state_*`

For each group, explain what evidence it provides. Confirm whether any exposed tool can modify a network device, approve a change, clear a session, reload a device, or push configuration.

If either namespace is missing, stop and name the missing namespace exactly.
