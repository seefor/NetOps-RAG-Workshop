Use only the already-configured `mcp_netops_rag_*` tools. Do not use `mcp_network_state_*` yet.

Do not search the MCP catalog, install an MCP server, or change MCP configuration. If the `mcp_netops_rag_*` namespace is unavailable, stop and report that exact problem.

The eBGP session between atl-core-r1 and nyc-edge-r1 is reported down, and the team suspects CHG-1234.

Using the approved runbook, indexed configuration snapshots, relevant change records, incident records, and postmortems:

1. Explain what should be checked.
2. Identify what the indexed knowledge suggests.
3. Identify what cannot be proven without current operational state.
4. Cite every source used.
5. Do not recommend or claim a production change.
