# Validation

Package validation performed before distribution:

- Python syntax compilation
- Core unit tests
- Hermes bonus unit tests
- Dataset statistics validation
- Shell syntax validation
- ZIP integrity validation

The final live integration path still depends on the attendee/instructor machine having Ollama, the required local models, Hermes Agent/Desktop, and the selected provider credentials installed and reachable.
