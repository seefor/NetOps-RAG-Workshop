# v8.2 — Hermes Desktop global MCP + Project reliability

- Moved workshop MCP registration out of the `netops-workshop` profile and into Hermes' global MCP registry, matching the tested Desktop/CLI behavior.
- Added `register_global_mcp.py` and actual `hermes mcp list/test` validation.
- Added automatic Hermes Project registration for **NetOps RAG Workshop**, including compatibility with positional project paths and `--path` builds.
- Preserved the exact active `.venv` Python executable for MCP stdio subprocesses.
- Removed stale v8/v8.1 workshop MCP entries from profile config during setup.
- Added explicit read-only MCP tool allowlists in global config.
- Updated Desktop preflight to validate profile, project, global MCP config, tool allowlists, MCP SDK v2 protocol, and live Hermes connectivity.
- Updated prompts and `netops-incident-investigation` skill to require the exact `mcp_netops_rag_*` and `mcp_network_state_*` namespaces and prohibit MCP catalog installation during incidents.
- Updated Desktop setup, demo, architecture, cross-platform, and troubleshooting docs.
- Bumped package version to 0.8.2.

# v8.1 — Hermes Desktop MCP reliability fixes

- Fixed MCP Python SDK v2 protocol smoke test: `Client.list_tools()` now reads `ListToolsResult.tools`.
- Fixed a macOS/Linux virtual-environment bug where resolving `sys.executable` followed the `.venv` Python symlink back to the base Homebrew/system interpreter. Hermes MCP servers now preserve the exact active virtual-environment Python path.
- Desktop preflight now verifies that both configured MCP server interpreters exist and match the active workshop Python.
- Added actionable repair instructions for stale profiles generated by v8.
- Bumped workshop package version to 0.8.1.

# Changelog

## v0.5.1 - Chroma reset compatibility

- Fixed `netops-rag ingest --data data --reset` on a brand-new Chroma database.
- `reset_collection()` now treats both `ValueError` and `chromadb.errors.NotFoundError` as the expected "collection does not exist yet" condition.
- Keeps `--reset` idempotent across Chroma versions.

## 0.5.0 — Reviewed workshop edition

- Fixed Streamlit package import failure with root entrypoint and absolute imports.
- Added project-root configuration and path handling.
- Added embedding provider/model tracking to Chroma collections.
- Disabled Chroma default embedding function because embeddings are supplied explicitly.
- Added clear legacy-index rebuild guidance and provider/model mismatch errors.
- Fixed double-overlap chunking behavior.
- Hardened retrieval and embedding response validation.
- Improved Streamlit-friendly runtime errors and preflight checks.
- Updated Hermes bonus to MCP Python SDK v2 and validated current Hermes filtering/skill config shape.
- Added Python 3.14 CI coverage and separate Hermes protocol CI job.


## 0.4.0 — Hermes NetOps Incident Copilot Bonus

- Added a complete optional Hermes Agent bonus under `bonus/hermes-netops-copilot/`.
- Added separate read-only MCP servers for NetOps RAG knowledge and synthetic network state.
- Added BGP policy-drift and branch IPsec certificate-expiry scenarios.
- Added a reusable `netops-incident-investigation` Hermes skill with references and report template.
- Added a Hermes configuration generator with MCP allowlists, skill discovery, backups, and skill-write approval.
- Added exact setup, presenter, demo, troubleshooting, security, and architecture guides.
- Added live-demo prompts, expected output, scenario switching, preflight checks, and unit tests.

## 0.3.0 — Instructor Command Runbook Edition

- Rebuilt `instructor/DEMO_SCRIPT.md` as an exact command-by-command delivery runbook.
- Added separate one-time setup and workshop-day startup sequences.
- Added terminal layout, expected results, teaching notes, checkpoints, and recovery commands.
- Added exact commands for all four labs, source-file inspection, Streamlit, evaluation, and provider switching.
- Added `scripts/preflight.py` to verify Python, packages, environment settings, Ollama, models, the CLI, and the dataset.
- Added `scripts/set_provider.py` for safe Ollama, OpenAI, and Anthropic switching.

## 0.2.0 — Expanded Four-Hour Edition

- Rewrote all four labs around explain, build, inspect, break, improve, and debrief.
- Expanded the synthetic dataset to 46 documents.
- Added 12 multi-vendor configuration examples.
- Added metadata filtering to CLI retrieval and Q&A.
- Added `stats` and `catalog` CLI commands.
- Added incident, postmortem, inventory, retired, decommissioned, rejected, lab-only, and untrusted sources.
- Added stale-source, source-conflict, and retrieved prompt-injection exercises.
- Added scenario cards, instructor answer key, and four-hour delivery guide.
- Updated slide storyboard to 36 slides.
- Updated cloud defaults to configurable current model IDs while keeping Ollama local-first.
- Added unit tests for document typing and metadata filter construction.

## v0.6.0 — Hermes Desktop Bonus

- Made Hermes Desktop the primary NetOps Incident Copilot bonus surface.
- Added macOS/Linux/Windows setup guidance.
- Added isolated `netops-workshop` profile setup.
- Added cross-platform Desktop launcher and Desktop-specific preflight.
- Rewrote the 15-minute bonus demo around streaming GUI tool activity, Desktop Skills, files, and integrated terminal.
- Preserved the original CLI workflow as fallback documentation.
- Added explicit Local-gateway guidance for local stdio MCP servers.
